7,2 overflow, underflow.
오버플로우가 발생하면 그 자료형의 최솟값부터 시작. 

7.3 자료형의 크기 구하기. 
sizeof 로 바이트 단위로 크기를 구할 수 있다. 

8.0 부동소수점 규약에 대해. 
- 부호, 가수(significand) 기수 base, 지수 exponent를 나누어 저장, 실수를 표현한다. 
- 컴퓨터는 기수가 2로 고정이다. 

8.1 실수의 표기.
- float는 숫자 뒤에 f를 붙임. double은 아무것도 안붙임. long double은 l을 붙임. 

이 실수 부분은 좀 빈약하게 넘어간 감이 있다. 그래도 이정도면...

9. char 는 아스키 문자 1자를 저장한다. 그도 그럴게 크기를 생각하면...
심지어 정수만으로 문자 표현이 가능하기도 하다. 16진수는 %X이다. hex의 X.
- 제어문자. /n, /r, /t도 아스키에 할당되어 있어서 각각 숫자를 지닌다. 